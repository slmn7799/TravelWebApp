import { OpacityscrollDirective } from './opacityscroll.directive';

describe('OpacityscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new OpacityscrollDirective();
    expect(directive).toBeTruthy();
  });
});
